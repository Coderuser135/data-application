import { ArrowRight, Check, ChevronRight, Dumbbell, Flame, HeartPulse, ShieldCheck, Sparkles, Star, Trophy, Users } from 'lucide-react';
import { Link } from 'react-router-dom';
import { useEffect } from 'react';
import { useDispatch, useSelector } from 'react-redux';
import Button from '@/components/ui/Button';
import Card from '@/components/ui/Card';
import SectionHeading from '@/components/ui/SectionHeading';
import { fetchMembershipPlans } from '@/redux/slices/membershipPlanSlice';
import { fetchProducts } from '@/redux/slices/productSlice';
import { formatCurrency } from '@/utils/formatters';

const features = [{ icon: Dumbbell, title: 'Purpose-built space', text: 'Every zone, rack, and machine is selected to help you train better.' }, { icon: Users, title: 'A focused community', text: 'Train alongside people who show up, put in the work, and keep growing.' }, { icon: Trophy, title: 'Progress that compounds', text: 'Simple systems and expert guidance turn effort into measurable results.' }];

export default function Home() {
  const dispatch = useDispatch();
  const plans = useSelector((state) => state.membershipPlans.items);
  const products = useSelector((state) => state.products.items);
  useEffect(() => { dispatch(fetchMembershipPlans()); dispatch(fetchProducts()); }, [dispatch]);
  return <div><section className="relative overflow-hidden bg-ink-950 pt-32 text-white"><div className="absolute inset-0 bg-hero-grid bg-[size:42px_42px] opacity-20" /><div className="absolute -right-40 top-20 h-[520px] w-[520px] rounded-full bg-brand-500/20 blur-[120px]" /><div className="relative mx-auto grid max-w-7xl items-center gap-12 px-5 pb-24 lg:grid-cols-[1.05fr_.95fr] lg:px-8 lg:pb-32"><div className="animate-fade-up"><div className="mb-7 inline-flex items-center gap-2 rounded-full border border-brand-400/30 bg-brand-400/10 px-3 py-1.5 text-xs font-semibold text-brand-300"><Sparkles size={13} /> Built for the long game</div><h1 className="max-w-3xl font-display text-5xl font-bold leading-[1.08] tracking-[-.04em] sm:text-6xl lg:text-7xl">Get stronger.<br /><span className="text-brand-400">Stay consistent.</span></h1><p className="mt-7 max-w-lg text-base leading-7 text-surface-300 sm:text-lg">A training environment designed for people who are serious about showing up, putting in the work, and becoming more capable every day.</p><div className="mt-9 flex flex-wrap gap-3"><Link to="/register"><Button size="lg">Start your journey <ArrowRight size={17} /></Button></Link><Link to="/about"><Button variant="outline" size="lg" className="border-white/20 text-white hover:border-brand-400 hover:text-brand-300">Discover IronForge</Button></Link></div><div className="mt-12 flex items-center gap-8 border-t border-white/10 pt-7"><div><p className="font-display text-2xl font-bold">5 AM</p><p className="mt-1 text-xs text-surface-400">Doors open daily</p></div><div className="h-10 w-px bg-white/15" /><div><p className="font-display text-2xl font-bold">100%</p><p className="mt-1 text-xs text-surface-400">Your effort matters</p></div></div></div><div className="relative hidden lg:block"><div className="relative ml-auto aspect-[.85] max-w-[460px] overflow-hidden rounded-[2rem] border border-white/10 bg-gradient-to-br from-brand-800 via-ink-800 to-ink-950 shadow-2xl"><div className="absolute inset-0 bg-[radial-gradient(circle_at_70%_20%,rgba(69,195,142,.35),transparent_35%),linear-gradient(135deg,transparent_45%,rgba(255,255,255,.06)_45%,transparent_46%)]" /><div className="absolute bottom-8 left-8 right-8"><p className="font-display text-4xl font-bold tracking-tight">DISCIPLINE<br /><span className="text-brand-400">OVER MOTIVATION.</span></p><div className="mt-5 flex items-center gap-2 text-sm text-surface-300"><Flame size={16} className="text-brand-400" /> Train with intent</div></div><Dumbbell className="absolute right-10 top-10 text-brand-400/40" size={100} strokeWidth={1} /></div><div className="absolute -bottom-5 -left-8 rounded-2xl border border-white/10 bg-ink-800/90 p-4 shadow-xl backdrop-blur"><div className="flex items-center gap-3"><span className="flex h-10 w-10 items-center justify-center rounded-xl bg-brand-500/15 text-brand-400"><HeartPulse size={20} /></span><div><p className="text-xs text-surface-400">Your next rep</p><p className="text-sm font-semibold">is the one that counts</p></div></div></div></div></div></section><section className="bg-white py-20 dark:bg-surface-950"><div className="mx-auto max-w-7xl px-5 lg:px-8"><SectionHeading eyebrow="Why IronForge" title="A better place to put in the work." description="No noise. No shortcuts. Just the space, tools, and people you need to make consistent progress." /><div className="mt-12 grid gap-5 md:grid-cols-3">{features.map(({ icon: Icon, title, text }) => <Card key={title} className="group transition hover:-translate-y-1 hover:shadow-lg"><span className="flex h-12 w-12 items-center justify-center rounded-xl bg-brand-100 text-brand-700 transition group-hover:bg-brand-500 group-hover:text-white dark:bg-brand-500/10 dark:text-brand-400"><Icon size={22} /></span><h3 className="mt-6 font-display text-lg font-semibold text-ink-950 dark:text-white">{title}</h3><p className="mt-3 text-sm leading-6 text-surface-600 dark:text-surface-400">{text}</p><Link to="/about" className="mt-6 inline-flex items-center gap-1 text-sm font-semibold text-brand-600">Learn more <ChevronRight size={15} /></Link></Card>)}</div></div></section><section className="bg-surface-100 py-20 dark:bg-surface-900"><div className="mx-auto max-w-7xl px-5 lg:px-8"><SectionHeading eyebrow="Memberships" title="Choose your level of commitment." description="Start where you are. Build from there. Every plan gives you the space to train with purpose." action={<Link to="/membership"><Button variant="outline">View all plans <ArrowRight size={16} /></Button></Link>} /><div className="mt-12 grid gap-5 md:grid-cols-3">{plans.slice(0, 3).map((plan, index) => <Card key={plan.id} className={`relative overflow-hidden ${index === 1 ? 'border-brand-500 shadow-lg shadow-brand-500/10' : ''}`}>{index === 1 && <span className="absolute right-5 top-5 rounded-full bg-brand-500 px-2.5 py-1 text-[10px] font-bold uppercase tracking-wider text-white">Popular</span>}<p className="text-sm font-semibold text-brand-600">{plan.name}</p><div className="mt-4 flex items-baseline gap-1"><span className="font-display text-4xl font-bold text-ink-950 dark:text-white">{formatCurrency(plan.price)}</span><span className="text-sm text-surface-500">/ {plan.duration_days >= 180 ? '6 mo' : plan.duration_days >= 90 ? '3 mo' : 'month'}</span></div><ul className="mt-6 space-y-3 border-t border-surface-200 pt-6 dark:border-surface-800">{(plan.features || []).slice(0, 4).map((feature) => <li key={feature} className="flex gap-2 text-sm text-surface-600 dark:text-surface-300"><Check size={16} className="mt-0.5 shrink-0 text-brand-500" />{feature}</li>)}</ul><Link to="/register"><Button variant={index === 1 ? 'primary' : 'secondary'} className="mt-7 w-full">Get started</Button></Link></Card>)}</div></div></section><section className="bg-brand-600 py-16 text-white"><div className="mx-auto flex max-w-7xl flex-col items-start justify-between gap-7 px-5 sm:flex-row sm:items-center lg:px-8"><div><p className="text-sm font-bold uppercase tracking-[.16em] text-brand-100">Your strongest chapter</p><h2 className="mt-2 font-display text-3xl font-bold tracking-tight sm:text-4xl">Starts with one decision.</h2></div><Link to="/register"><Button variant="secondary" size="lg">Join IronForge <ArrowRight size={17} /></Button></Link></div></section></div>;
}
